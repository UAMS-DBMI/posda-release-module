/** Stage 5 — landing pages and go-live. Wired up in step 7, once WordPress
 *  state is in the cycle payload. */
export default function DisseminateStage() {
  return (
    <p className="text-sm" style={{ color: "var(--muted)" }}>
      WordPress landing pages and the bulk go-live switch land here in a later
      step.
    </p>
  );
}
